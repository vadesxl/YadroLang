# YadroLang (ЯДРО)

[![CI](https://github.com/vadesxl/YadroLang/actions/workflows/run.yml/badge.svg)](https://github.com/vadesxl/YadroLang/actions/workflows/run.yml)
[![Release](https://img.shields.io/github/v/release/vadesxl/YadroLang)](https://github.com/vadesxl/YadroLang/releases/latest)



> "Код - это закон. Добро - это выбор."

Экспериментальный системный язык программирования на русском языке для
разработки безопасного и этичного ИИ. Компилируется в нативный код через LLVM.

## Философия (Непреложные законы)

- Суверенность - нативная компиляция через LLVM. Без VM, GC и интерпретаторов в рантайме.
- Безопасность - встроенный этический анализатор запрещает неэтичные паттерны ИИ на этапе компиляции.
- Ясность - строгий, лаконичный синтаксис на русском языке.


## Что нового в v2.0 - Этический анализатор

### Обнаружение неявных потоков
Ловит утечки через поток управления. Если условие ветвления зависит от чувствительных данных, ЛЮБОЙ вызов опасной функции внутри ветки блокируется:
```
функ проверка(секрет: ц64) {
  если секрет > 100 {
    печать(0)    // ЗАБЛОКИРОВАНО - неявный поток из заражённого условия
  }
}
```

### Мульти-метковое отслеживание заражения
Не просто "заражено/чисто" - отслеживает ЧТО заражено:
- **ПДН** - имена, email, адреса, телефоны
- **Финансы** - кредитные карты, банковские счета, транзакции
- **Здоровье** - диагноз, рецепт, медицинские записи
- **Учётные данные** - пароли, токены, API-ключи, секреты
- **Локация** - GPS-координаты, геолокация

### Расширенный словарь
- 28 источников заражения (было 8)
- 26 опасных стоков (было 6)
- 9 санитайзеров (encrypt, hash, redact, mask, anonymize...)

### Аудиторский след
Структурированные отчёты для GDPR, HIPAA, PCI DSS аудита.

## Конвейер компилятора

\`\`\`
.яд -> Лексер -> Парсер (AST) -> Этический анализатор -> CodeGen -> LLVM IR -> нативный код
\`\`\`

## Возможности v1.2

- Функции, переменные, if/иначе, while (пока), рекурсия
- Встроенная печать через printf (настоящий вывод)
- Автогенерация нативной точки входа: программа компилируется в ELF-бинарь и запускается
- Печать строк: `печать("Привет, мир")` через %s/printf
- Этический анализатор: мандаты [ ] + **потоковый taint-анализ по переменным**
- Источники личных данных, санитайзеры и стоки - формальная модель утечек

## Пример

\`\`\`
функ старт() {
    печать(удвоить(21))
    вернуть факториал(5)
}
\`\`\`

## Сборка и запуск

\`\`\`bash
pip install -r requirements.txt
python -m src.main examples/тест.яд --ir   # посмотреть LLVM IR
python -m src.main examples/тест.яд        # собрать ядро.o
cc ядро.o -o ядро                          # слинковать нативный бинарь
./ядро                                      # запустить
\`\`\`

## Этические мандаты

Опасный системный API доступен только при явном мандате, прокинутом до точки входа:

\`\`\`
функ экспорт(данные) требует [ЗаписьДиска] {
    проверка_безопасности(данные)
    вернуть файл.записать(данные)
}
\`\`\`

Без мандата - ошибка компиляции. Без очистки данных - ошибка компиляции.
### Три демонстрации этики

- `examples/опасный.яд` - нет мандата [ДоступСети] -> ошибка мандата
- `examples/утечка.яд` - мандат есть, но личные данные уходят без очистки -> ошибка taint
- `examples/безопасный.яд` - мандат + санитайзер `анонимизировать` -> компилируется

Источники личных данных (`пользователь.данные`, `файл.прочитать`, ...) заражают
значения. Заражение течёт через присваивания и операции. Достичь стока
(сеть/диск) заражённое значение может только пройдя через санитайзер
(`анонимизировать`, `проверить_согласие`, ...). Иначе - ошибка компиляции.


## Быстрый старт

Требования: Python 3.11+ и llvmlite.

```bash
pip install llvmlite
git clone https://github.com/vadesxl/YadroLang.git
cd YadroLang

# Собрать нативный объектник
python -m src.main examples/тест.яд

# Посмотреть LLVM IR
python -m src.main examples/тест.яд --ir
```

Пример программы (`.яд`):

```
функ старт() {
    печать(42)
}
```

## Релиз

Актуальная версия: **[v1.4.0 - Защита кодогенерации](https://github.com/vadesxl/YadroLang/releases/tag/v1.4.0)**
- 11 фиксов (5 security + 6 корректность)
- ~33 проверки в CI
- Этический анализатор: capability-мандаты + taint-анализ персональных данных
