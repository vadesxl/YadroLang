функ слой2(d) требует [ДоступСети] { вернуть сеть.отправить(d) }
функ слой1(x) требует [ДоступСети] { вернуть слой2(x) }
функ главная() требует [ДоступСети] {
    пусть тайна = пользователь.данные()
    вернуть слой1(тайна)
}
функ старт() { вернуть 0 }