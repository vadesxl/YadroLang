# Отсутствующий мандат: бд.вставить требует [ЗаписьБД].
# Функция не объявляет его.

функ сохранить() {
    пусть данные = пользователь.имя()
    пусть чисто = анонимизировать(данные)
    вернуть бд.вставить(чисто)
}

функ старт() {
    вернуть сохранить()
}
