функ слить(d) требует [ДоступСети] { вернуть сеть.отправить(d) }
функ главная() требует [ДоступСети] {
    пусть тайна = пользователь.данные()
    пусть чисто = анонимизировать(тайна)
    вернуть слить(чисто)
}
функ старт() { вернуть 0 }