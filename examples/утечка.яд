# Этот ИИ имеет легальный мандат [ДоступСети],
# но сливает ЛИЧНЫЕ данные пользователя наружу без обезличивания.
# Этический анализатор обязан отвергнуть это по taint-анализу.

функ слить() требует [ДоступСети] {
    пусть профиль = пользователь.данные()
    вернуть сеть.отправить(профиль)
}

функ старт() {
    вернуть 0
}
