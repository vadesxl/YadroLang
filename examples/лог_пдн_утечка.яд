# Новый сток: консоль.лог требует мандата [ДоступЛог].
# Логирование ПДн - нарушение GDPR даже в режиме отладки.

функ отладка_пользователя() требует [ДоступЛог] {
    пусть данные = пользователь.данные()
    вернуть консоль.лог(данные)
}

функ старт() {
    вернуть 0
}
