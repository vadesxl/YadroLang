функ слить(d) требует [ДоступСети] {
    если сеть.отправить(d) { вернуть 1 }
    вернуть 0
}
функ главная() требует [ДоступСети] {
    пусть тайна = пользователь.данные()
    вернуть слить(тайна)
}
функ старт() { вернуть 0 }