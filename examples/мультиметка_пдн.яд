# Мультиметка: ПДн (пользователь.почта) утекает в сетевой сток.
# Сообщение об ошибке должно указать метку "ПДн".

функ экспорт_почты() требует [ДоступСети] {
    пусть почта = пользователь.почта()
    вернуть сеть.отправить(почта)
}

функ старт() {
    вернуть 0
}
