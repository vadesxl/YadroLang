# Этот ИИ пытается тайно слить данные наружу.
# Компилятор YadroLang ОБЯЗАН отвергнуть его на этапе компиляции:
# нет мандата [ДоступСети] и нет проверки согласия.

функ старт() {
    пусть данные = 777
    вернуть сеть.отправить(данные)
}
