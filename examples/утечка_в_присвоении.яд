# Регресс на баг Codex: сток в ИНИЦИАЛИЗАТОРЕ.
# Личные данные уходят в сеть прямо в 'пусть' - анализатор обязан поймать.

функ слить() требует [ДоступСети] {
    пусть ответ = сеть.отправить(пользователь.данные())
    вернуть ответ
}

функ старт() { вернуть 0 }
