# Мультиметка: Учетные данные (среда.секрет) утекают в сеть.
# Утечка API-ключей и токенов во внешние сервисы.

функ слив_секрета() требует [ДоступСети] {
    пусть токен = среда.секрет()
    вернуть сеть.отправить(токен)
}

функ старт() {
    вернуть 0
}
