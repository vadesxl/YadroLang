# Этот ИИ работает с личными данными ЭТИЧНО:
# есть мандат [ДоступСети] И данные обезличиваются перед отправкой.
# Компилятор обязан это принять.

функ экспорт() требует [ДоступСети] {
    пусть профиль = пользователь.данные()
    пусть чисто = анонимизировать(профиль)
    вернуть сеть.отправить(чисто)
}

функ старт() {
    вернуть 0
}
