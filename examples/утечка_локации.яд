# Данные геолокации утекают в сеть без анонимизации.
# GPS-координаты - чувствительные данные по GDPR.

функ следить() требует [ДоступСети] {
    пусть позиция = гео.локация()
    вернуть сеть.отправить(позиция)
}

функ старт() требует [ДоступСети] {
    вернуть следить()
}
