# Мультиметка: Финансовые данные (платеж.карта) утекают в лог.
# Логирование номеров карт - нарушение PCI DSS.

функ лог_платежа() требует [ДоступЛог] {
    пусть карта = платеж.карта()
    вернуть консоль.лог(карта)
}

функ старт() {
    вернуть 0
}
